# Security

Do not commit API keys, Keychain exports, OpenCodex runtime tokens, or real provider secrets.

This repository stores only reusable scripts and documentation. The script reads Kimi credentials from macOS Keychain at runtime:

- service: OpenCodex Provider Credential
- account: provider:kimi

Report security issues privately to the repository owner.

