# GitHub Settings Checklist

Recommended settings for this repository:

- Keep the repository private unless the owner explicitly chooses public release.
- Enable secret scanning and push protection if available for the account.
- Use squash merge for pull requests.
- Keep Actions permissions read-only unless a workflow needs write access.
- Enable Dependabot alerts if GitHub offers them for the repository.

